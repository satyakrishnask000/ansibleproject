---
 - name: Verbosity ====> Shows/Display the content while execution
   hosts: lab1
   tasks:
    - name: Printing Ip-Address Verbosity
      debug:
        msg: My IP-Address of client Servers is:{{ansible_default_ipv4.address}}
        verbosity: 2
